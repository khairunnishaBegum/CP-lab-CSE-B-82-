#include<stdio.h>
int main(){
  int n;
  scanf("%d", &n);
  int freq[100] = {0};
  int x;
  for (int i = 0; i < n; i++) {
   scanf("%d", &x);
   freq[x] = freq[x] + 1;
  }
  for (int i = 0; i < 100; i++) {
    printf("%d ", freq[i]);
   }
  return 0;
}
