#include <stdio.h>
int main() {
    int x, y;
    scanf("%d %d", &x, &y);
    if (y == 0) {
        printf("Division by zero");
        return 0;
    }
    int low = 0, high = x, mid;
    while (low <= high) {
        mid = (low + high) / 2;
        if (mid * y == x) {
            printf("%d", mid);
            return 0;
        }
        else if (mid * y < x) {
            low = mid + 1;
        }
        else {
            high = mid - 1;
        }
    }
    return 0;
}
